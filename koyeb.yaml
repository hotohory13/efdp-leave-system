name: efdp-leave-system
type: web
image: docker.io/library/python:3.11-slim
build:
  command: "pip install -r requirements.txt && python -m scripts.seed_data --demo"
run:
  command: "uvicorn app.main:app --host 0.0.0.0 --port 8000"
ports:
  - port: 8000
    path: /
